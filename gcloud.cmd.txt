# to deploy the job:
gcloud beta run jobs deploy isin-estractor --env-vars-file=./gcloud.dev.env --source=. --execute-now --region=europe-west1 \
--add-volume name=gcs-1,bucket=rws-data,type=cloud-storage \
--add-volume-mount volume=gcs-1,mount-path=/data

# To execute this job again, use:
gcloud beta run jobs execute isin-estractor

1. webclaw --urls-file <issuer>.urls.txt --output-dir output\<dt> --only-main-content
> output\<dt> contains files as <isin>.md
2. extract2dbms --config <issuer>.kv.txt --input-dir output\<dt> --output_table <tablename>
> <tablename> contains entries as defined in <issuer>.kv.txt, eg. <isin>,<dt>,<ask>,<bid>,...
3. dbms2rest